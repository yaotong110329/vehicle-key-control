{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default permissions for the main Vehicle Key Control window",
  "windows": ["main"],
  "permissions": ["core:default"]
}
